# Core (cross-platform)
PySide6>=6.6
mss>=9.0
Pillow>=10.0
numpy>=1.24
deep-translator>=1.11
pynput>=1.7

# macOS OCR (Apple Vision) + window tweaks for GeForce Now fullscreen overlay
ocrmac>=1.0 ; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0 ; sys_platform == "darwin"

# Cross-platform OCR engine (PaddleOCR via ONNX). This is the ONLY OCR backend off
# macOS (Apple Vision is macOS-only), so it auto-installs on Windows/Linux. On macOS
# it's optional — install it by hand only if you want a fallback engine for Vision.
rapidocr-onnxruntime>=1.3 ; sys_platform != "darwin"

# Offline translation (Argos Translate; installed on demand by Settings -> Offline
# model -> Download). Uncomment to preinstall the "offline" engine:
# argostranslate>=1.9
